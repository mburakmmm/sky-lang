# Smaller Fibonacci for accurate timing
import _bypass

function fib(n)
  if n <= 1
    return n
  end
  return fib(n - 1) + fib(n - 2)
end

function main()
  let n = 30
  print("Computing fib(30)...")
  let result = fib(n)
  print("Result:", result)
end

