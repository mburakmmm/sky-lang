# TEST: Import system
# Expected: Should work according to .cursorrules

import math
import fs as filesystem

function main
  let result = math.sqrt(16)
  print("Math result: " + result)
  
  let files = filesystem.listDir(".")
  print("Files: " + len(files))
end

