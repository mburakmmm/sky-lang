# TEST: Unsafe blocks
# Expected: Should work according to .cursorrules

function dangerousOperation
  unsafe
    let ptr = 0x1000
    let value = ptr + 100
    return value
  end
end

function main
  let result = dangerousOperation()
  print("Unsafe result: " + result)
end

